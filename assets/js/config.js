/* ============================================================================
   Wisp configuration
   To connect the live backend, paste your Supabase project's URL and public
   "anon" key below. Find them in Supabase under: Project Settings > API.

   Both values are safe to commit. The anon key is meant to be public; your data
   is protected by Row Level Security in the database, not by hiding the key.

   Leave them blank to run the site in demo mode (mock data, no real accounts).
   ==========================================================================*/
window.WISP_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
